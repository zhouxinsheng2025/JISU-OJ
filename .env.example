# JISU裁判系统配置文件
# 复制为 .env 并修改

# 数据库 (SQLite默认，PostgreSQL示例注释)
DATABASE_URL=sqlite+aiosqlite:///judge.db
# DATABASE_URL=postgresql+asyncpg://user:pass@localhost/judge

# JWT密钥 (务必修改为随机字符串！)
JWT_SECRET=change-me-in-production
JWT_EXPIRE_HOURS=24

# 判题配置
JUDGE_POLL_INTERVAL=1
COMPILE_TIME_LIMIT=30
